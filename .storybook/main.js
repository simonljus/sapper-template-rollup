
const path = require('path');

// Export a function. Accept the base config as the only param.
module.exports = {
    stories: ['../src/**/*.stories.[tj]s']
    //addons: ['@storybook/addon-knobs/register'] /*for addons */
};